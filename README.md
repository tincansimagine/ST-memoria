# Memoria — 장기기억 서고

SillyTavern용 장기기억 확장입니다. 대화를 턴 단위로 자동 기록해 구조화된 장기기억으로 보관하고,
매 턴 현재 맥락과 관련 있는 기억만 골라 **토큰 예산 안에서** 프롬프트에 주입합니다.

외부 백엔드·별도 프로그램·임베딩 API가 **전혀 필요 없습니다.** 실리태번 안에서 완결됩니다.

## 설치

1. 이 폴더를 `SillyTavern/data/<user>/extensions/` 아래에 복사하거나,
   확장 탭 → "Install extension"에서 저장소 주소로 설치합니다.
2. 확장 탭에서 **Memoria — 장기기억 서고** 패널을 엽니다.
3. (권장) 설정 탭에서 기록용 **연결 프로필** 또는 **커스텀 API**를 지정합니다. 지정하지 않으면 현재 연결된 API를 그대로 사용합니다.
   기록은 백그라운드에서 턴마다 일어나므로 저렴하고 빠른 모델을 권장합니다.

## 작동 방식

```
AI 응답 완료 ──► [사서] 턴을 구조화된 기억으로 정리해 서고에 보관 (백그라운드, 채팅을 막지 않음)
                  · 기억 10종: 사건/관계/사실/취향/약속/목표/물건/장소/비밀/인상
                  · 인물 도감(프로필·관계), 캐논(세계 설정 키-값), 상태 보드(슬롯별 최신값), 서약
                  · 인용문은 원문의 실제 부분 문자열일 때만 저장 (환각 증거 차단)
                  · N턴마다 "지금까지의 이야기" 자동 요약 → 오래되면 연대기로 병합

사용자 입력 ──► [회상] 로컬 해시 임베딩 + 어휘 일치 + 중요도 + 최신성으로 채점
                  · MMR 다양화, 종류/턴별 상한, 다중 질의(최근 메시지) 보정
                  · 오래된 기억은 "화석화"되어 더 강한 관련성이 있어야 소환됨
             ──► [주입] 토큰 예산에 맞춰 단계적 축소 후 시스템 메시지로 주입
                  · 서약(비밀 유지·미해결 떡밥 등)은 마지막까지 유지
```

### 비밀과 서브텍스트 (지식 경계)

기억에는 공개(`public`) / 비공개(`private`) / 비밀(`secret`) 등급과 소유자가 있습니다.
현재 캐릭터가 소유한 비공개 기억은 **"서브텍스트 전용 — 직접 발설 금지"** 지시와 함께 주입되고,
권한이 없는 기억은 개수만 알려 추측을 금지합니다. 비밀을 아는 캐릭터는 의미심장하게 행동할 수 있지만,
모르는 캐릭터가 갑자기 아는 것처럼 구는 누출을 막습니다.

### 일관성 보장

- **스와이프/리롤**: 해당 턴의 파생 기억이 자동 무효화되고 새 응답으로 재기록됩니다.
- **메시지 편집**: 편집된 턴을 다시 기록합니다.
- **메시지 삭제**: 저장된 턴을 실제 채팅과 내용 기준으로 대조합니다. 앞쪽 메시지가 삭제돼 번호가 밀린 턴은 같은 내용의 메시지에 자동 재연결되고, 정말 사라진 턴의 기억만 정리됩니다.
- 기억은 **채팅별로** 저장됩니다(`chat_metadata`). 채팅 파일과 함께 이동/백업됩니다.

## 주요 기능

| 기능 | 설명 |
|---|---|
| 자동 기록 | 턴마다 백그라운드로 구조화 기억 추출 (끄고 수동 기록만 쓸 수도 있음) |
| **서고에 질문** | "우리 처음 만난 게 언제였지?" — 저장된 기억만 근거로 턴 번호를 인용해 답변 (`/memoria-ask`) |
| **스토리 바이블** | 인물·캐논·서약·타임라인·요약을 한 편의 마크다운 문서로 내보내기 |
| 기억 브라우저 | 검색·종류 필터·편집·삭제·비활성화·**고정**(매 턴 반드시 주입) |
| **인물 도감** | 등장인물의 프로필(역할·나이·직업·외모·특성)과 **인물별 관계**를 자동 추적·편집 |
| 세계 탭 | 캐논(세계 설정) / 상태 보드 / 서약 관리 |
| **현재 장면 한 줄** | 장소·날짜·시각을 자동 추적해 세계 탭 상단과 주입에 한 줄로 표시 (`Scene Now`) |
| 요약 | N턴마다 자동 요약(이전 요약 참조로 일관성 유지), 오래된 요약은 연대기로 병합, 수동 요약/편집 가능 |
| **턴별 기록** | 사서가 남긴 턴 단위 기록을 요약 탭에서 확인·수정 — 고치면 다음 요약부터 반영 |
| **이벤트 연표** | 고백·폭로·죽음 등 이야기의 결정적 전환점만 골라 시간순 연표로 자동 기록 |
| **아이템 도감** | 의미 있는 선물·전개상 중요한 물건의 소유자·상태를 자동 추적 |
| **배경 참고** | 카드·페르소나·월드인포(로어북)를 사서·요약의 배경 자료로 제공 (선택) |
| **임베딩 API** | OpenAI 호환 임베딩 지정 시 의미 검색 정확도 대폭 향상 (선택, 실패 시 자동 폴백) |
| **서고 정리** | N턴마다 중복 기억 병합·소음 제거·중요도 재조정 (수동 실행 가능) |
| **서약 자동 해소** | 화면상 해소된 서약을 사서가 자동 ✔ 처리 (스와이프 시 롤백) |
| **자동 숨김** | 요약이 커버한 옛 메시지를 채팅에서 숨겨 컨텍스트 토큰 대폭 절약 (해제 가능) |
| **자료실** | 원작 소설·설정 문서를 붙여넣으면 매 턴 관련 대목만 골라 배경 참고로 주입 (채팅 사실이 항상 우선) |
| **인계** | 다른 채팅의 요약·고정 기억·인물·캐논·서약·자료실을 새 채팅으로 이어받기 (A→B→C 누적) |
| **요약 언어** | 자동/한국어/영어/일본어/하이브리드(영어 서술 + 대사 원문 유지) |
| 과거 채팅 색인 | 미기록 턴 일괄 기록, **범위 지정**(#시작~#끝)과 중단, **실패 턴 자동 재시도** |
| 장면 연출 (실험적) | 생성 직전 연출 LLM이 장면의 목표·피해야 할 전개를 계획해 함께 주입 |
| **커스텀 API** | 사서 전용으로 OpenAI 호환 API(URL/키/모델) 직접 지정 + 연결 테스트 |
| 백업/복원 | 채팅 기억 전체를 JSON으로 내보내기/가져오기 |
| 프롬프트 편집 | 사서/연출/요약/연대기 프롬프트 자유 수정 + 기본값 복원 |

## 슬래시 명령어 · 매크로

- `/memoria` — 켜기/끄기 토글 (`/memoria on`, `/memoria off`)
- `/memoria-remember 내용` — 수동 기억 추가 (자동 정리에서 제외)
- `/memoria-search 검색어` — 기억 검색
- `/memoria-ask 질문` — 저장된 기억만 근거로 스토리 질문에 답변
- `/memoria-index` — 현재 채팅 전체 색인
- `{{memoria_story}}` — 지금까지의 이야기 요약 전체
- `{{memoria_count}}` — 저장된 기억 수

## 설정 가이드

| 설정 | 기본값 | 설명 |
|---|---|---|
| 주입 토큰 예산 | 8000 | 주입되는 장기기억의 최대 토큰. 초과 시 자료실→인용→회상→오래된 요약→상태 순으로 축소 |
| 주입 깊이 | 1 | 채팅 내 주입 위치 (0 = 마지막 메시지 바로 앞) |
| 회상 개수 | 10 | 턴당 회상할 기억 수 |
| 요약 주기 | 8턴 | 자동 요약 간격 |
| 요약 언어 | 자동 | 요약·기억이 기록될 언어. 하이브리드는 영어 서술 + 대사 원문 유지 |
| 이전 요약 참조 수 | 3 | 새 요약 생성 시 참조할 이전 요약 수 (0 = 안 함, -1 = 전체) |
| 카드·페르소나 참고 | 켜짐 | 사서·요약이 캐릭터 카드·페르소나 요지를 배경 자료로 참고 |
| 월드인포 참고 | 꺼짐 | 활성 로어북을 배경 자료로 참고 (로어북이 크면 토큰 증가) |
| 자동 숨김 | 꺼짐 | 요약이 커버한 옛 메시지를 숨김. 최근 메시지 보존 수(기본 6)만큼은 항상 표시 |
| 최대 기억 수 | 400 | 초과 시 중요도 낮은 오래된 기억부터 정리 (고정/수동 제외) |
| 사서 최대 토큰 | 30000 | 사서 응답 상한. 요약·기록이 중간에 잘리지 않도록 넉넉하게 |
| API 소스 | 실리태번 | 사서가 쓸 API. 실리태번(현재 API/연결 프로필) 또는 커스텀 OpenAI 호환 API |
| 임베딩 소스 | 끄기 | 의미 검색 강화. 로컬(실리태번 내장 · 무료) 또는 외부 OpenAI 호환 임베딩 API |

## 팁

- **기존 채팅에 도입**: 도구 탭 → "색인 시작"으로 지난 대화를 한 번에 기억화하세요. 범위(#시작~#끝)를 지정할 수도 있습니다.
- **토큰이 부족하면**: 설정 탭의 "요약된 메시지 자동 숨김"을 켜세요. 요약이 커버한 옛 메시지가 컨텍스트에서 빠지고, 그 내용은 "지금까지의 이야기"와 회상으로 대체됩니다.
- **새 채팅방/봇카드 교체 시**: 이전 채팅에서 "내보내기" → 새 채팅에서 "인계 파일 가져오기"를 하면 이야기·기억이 그대로 이어집니다. 인계는 계속 누적됩니다.
- **설정이 헷갈릴 때**: 개요 탭의 "서고에 질문"으로 스토리 사실 관계를 바로 확인하세요. 기억에 없는 건 없다고 답합니다.
- **중요한 설정을 잊게 하고 싶지 않다면**: 기억 브라우저에서 📌 고정하거나 `/memoria-remember`를 쓰세요.
- **떡밥 관리**: 세계 탭의 서약에서 해결된 떡밥은 ✔로 처리하면 주입에서 빠집니다.
- **기록 실패가 떴다면**: 상태줄에 ⚠ 표시가 보이면 도구 탭에서 색인을 다시 실행하면 실패한 턴만 재시도합니다.

## v1.0.1

- 세계 탭의 **아이템 도감·서약에 수기 추가 버튼** — 아이템은 이름·의미·소유자·상태를, 서약은 종류·내용·우선순위를 직접 등록. 수기 항목은 스와이프 롤백에서 보호됨
- 연결 테스트 토큰 예산 20 → 2048 (Gemini 등 추론 모델이 생각 토큰만 쓰다 빈 응답으로 잘려 "연결 실패"가 뜨던 문제), 실패 시 실제 원인 메시지 표시
- 커넥션 프로필로 Vertex AI를 쓸 때 인증 방식(JSON 서비스 계정)이 전달되지 않아 express 모드 키 오류가 나던 문제 수정

## v1.0.0

- **자료실**: 세계 탭에 배경 참고 문서 서가 추가 — 원작 소설·설정집을 붙여넣으면 문단 단위 대목으로 저장되고, 매 턴 지금 장면과 관련된 대목만 골라 별도 구역(`Source Excerpts`)으로 주입. 채팅에서 확정된 사실이 항상 우선하며 토큰 예산이 부족하면 가장 먼저 축소. 인계에도 포함
- **상태 변경 이력**: 상태 보드·캐논이 값이 바뀔 때 이전 값을 함께 보관 — 최근 바뀐 상태는 주입에 `(was: 이전값)`으로 표시돼 낡은 기억과의 혼동을 방지하고, 세계 탭에서도 이전 값이 보임
- **최신성 보호**: 과거 턴을 재기록(색인·재시도)해도 더 새로운 턴이 기록한 상태·캐논을 덮어쓰지 않음
- **스와이프 롤백 개선**: 스와이프·삭제로 턴 기록이 무효화되면 그 턴이 바꾼 상태·캐논이 이전 값으로 되돌아감 (슬롯 자체가 사라지지 않음)
- **사서 규율 강화**: ① 수치 날조 금지 — 정확한 새 값이 화면에 안 나왔으면 방향만 기록 ② 유저 입력은 "의도", 본문 묘사만 "일어난 일"로 기록 ③ 세계에 없는 것(no_magic 등)도 캐논으로 고정 가능 ④ 퇴장 인물의 행선지(whereabouts) 추적 ⑤ 중요도 채점 기준 명문화("잊으면 이야기가 깨지는가")
- **지식 경계 규율**: 의심·소문·전언은 저절로 사실이 되지 않음 — 장면이 직접 보여준 것만 사실로, 들은 것은 "누구의 앎"으로 기록 ("X가 Y에게 왕이 죽어간다고 말했다" ≠ "왕이 죽어간다"). 요약·연대기도 압축하면서 확신 등급을 올리지 못하게 봉인 ("~라는 소문", "~를 시도했다" 유지), 서고 질문 답변도 믿음은 믿음으로 답함
- **인물 정체 규율**: 이름·직함이 같아도 다른 사람은 합치지 않고, 표기만 다른 같은 사람(별명·로마자)은 쪼개지 않음. 언급만 된 집·직장을 현재 장면 위치로 오인하지 않음
- **기록 절제**: "애매하면 기록하지 말 것" — 흐릿한 다건보다 정확한 소수 우선
- **구조형 요약**: "지금까지의 이야기"가 한 문단 통짜에서 3구획 레코드로 — `PLOT`(무슨 일이, 인과 서사) / `SHIFTS`(장면이 끝나도 남는 변화) / `OPEN`(미해결). 메인 모델이 훑어 읽기 좋아지고, 연대기 병합 시 이미 해소된 SHIFTS/OPEN은 자동 탈락. 기존 문단형 요약과 섞여 있어도 정상 동작
- **삭제 시 기억 보호**: 앞쪽 메시지를 삭제해 번호가 밀려도, 이후 턴들을 내용 기준으로 다시 찾아 재연결 — 삭제 한 번에 뒷턴 기억이 전부 증발하던 문제 해결
- **낡은 회상 꼬리표**: 회상된 기억이 "그 후 바뀐 상태"의 옛 값을 담고 있으면 `[as of then — 지금 값]` 꼬리표를 붙여 주입 — 옛 기억이 현재 상태를 덮어쓰는 혼동 방지
- **장면 연속 회상**: "응", "계속해줘" 같은 짧은 입력에도 직전 본문의 장면을 보조 질의로 써서 맥락에 맞는 기억을 소환
- **사서 형식 재시도**: JSON 형식을 못 지키는 모델이면 형식 경고를 덧붙여 자동 1회 재시도 — 저성능 모델에서도 기록 실패율 감소
- **떡밥 절제**: 80턴 넘게 진전 없는 미해결 떡밥은 주입에 한 번에 2개까지만 (서고에는 그대로 유지, 비밀·설정 한계는 항상 전부 유지)
- 인용문 검증이 공백 차이를 허용하도록 개선 (줄바꿈 때문에 멀쩡한 인용이 버려지던 문제)
- 주입 토큰 예산 기본값 4000 → **8000** (기억·요약·자료실이 넉넉히 실리도록. 직접 바꾼 값은 유지)

## v0.8.0

- **로컬 임베딩 (무료)**: 임베딩 소스에 "로컬 (실리태번 내장)" 추가 — 실리태번 서버가 transformers.js로 임베딩을 직접 계산. API 키·비용 없이 의미 검색 정확도 향상. 첫 사용 시 모델(수십 MB) 1회 다운로드
- 임베딩 설정이 켜기/끄기에서 **소스 선택**(끄기 / 로컬 / 외부 API)으로 변경 — 기존 "임베딩 API 사용" 설정은 자동 이전
- 임베딩 URL 정규화 개선: Gemini OpenAI 호환 경로(`.../v1beta/openai`)도 인식
- 기억이 바뀌면(추가·수정·삭제·서고 정리) 로컬 색인이 자동으로 따라감

## v0.7.0

- **임베딩 API (선택)**: OpenAI 호환 임베딩 API를 지정하면 의미 검색 정확도가 크게 향상 — 동의어·의역까지 회상. 실패 시 내장 해시 검색으로 자동 폴백, "기억 전체 재계산" 버튼 제공
- **서고 정리 (기억 통합)**: N턴마다(기본 30, 0=끔) 사서가 중복 기억 병합·소음 제거·중요도 재조정. 고정·수동 기억은 보호. 도구 탭에서 수동 실행도 가능
- **검색 힌트**: 사서가 기억마다 "나중에 이 기억을 소환할 만한 단서 키워드"를 함께 저장 — 회상 매칭력 강화
- **서약 자동 해소**: 활성 서약 목록을 사서에게 전달, 이번 턴에 화면상으로 해소된 서약을 자동으로 ✔ 처리 (스와이프 시 자동 롤백)
- **과거 회상형 질의 인식**: "우리 처음 만났을 때" 같은 질문이면 최신성 페널티를 해제해 오래된 기억도 동등하게 소환
- 기록·요약 비차단화: 사서·요약 호출이 전송 잠금 없이 백그라운드로 실행 — 기록 중에도 다음 채팅 가능
- 모든 개별 삭제 버튼에 확인 팝업 추가, 확장 패널 초기 접힘 상태 수정, 인물 카드 아이콘 정렬 수정
- 사건(episode) 기억이 원문을 통째로 복사하던 문제 수정 (기존 데이터 자동 정리 포함)

## v0.5.0

- **배경 참고**: 캐릭터 카드·페르소나(기본 켜짐), 월드인포/로어북(선택)을 사서·요약의 배경 자료로 제공 — 이름·설정 오해 감소
- **이전 요약 참조**: 새 요약을 만들 때 이전 요약을 참조해 시간·장소·관계 표기의 일관성 유지 (참조 개수 설정: 0 / N / 전체)
- **추적 도감 확장**: 인물 도감에 더해 **이벤트 연표**(결정적 전환점만 시간순 기록)와 **아이템 도감**(중요한 물건의 소유자·상태 추적) 추가 — 켜면 매 턴 주입에 포함되고 인계·스토리 바이블에도 실림
- **현재 장면 한 줄**: 상태 보드에 쌓이는 장소·날짜·시각을 골라 세계 탭 상단과 주입 맨 앞에 한 줄로 정리 (`Scene Now — place · date · time`)
- **기록·요약 비차단화**: 사서·요약 호출이 ST 생성 파이프라인(전송 잠금)을 점유하지 않도록 raw 호출로 전환 — 기록이 도는 중에도 다음 채팅을 바로 보낼 수 있음. 장면 연출도 이제 별도 프로필 없이 동작
- **턴별 기록 뷰**: 요약 탭에서 턴 단위 기록을 확인·수정 — 수정하면 다음 요약부터 반영
- 사서 프롬프트 전면 재작성 ("다섯 개의 서가" 구조) — 서고 컨셉으로 완전히 재구성

## v0.4.0

- **서고에 질문**: 저장된 기억·요약·캐논만 근거로 스토리 질문에 답변. 턴 번호 인용, 기억에 없으면 없다고 답함 (개요 탭 + `/memoria-ask`)
- **스토리 바이블 내보내기**: 인물 도감·캐논·서약·타임라인·지금까지의 이야기를 마크다운 문서 한 편으로 정리 (LLM 호출 없이 즉시)
- 사서 최대 토큰 기본값 1024 → **30000** (요약·기록이 잘리는 문제 방지), 주입 토큰 예산 기본값 1600 → 4000
- 청크/연대기 요약 길이 상한 확대, 연출 계획 토큰 상한 확대
- 용어 전면 정리: 서고 컨셉에 맞게 주입 문서·기억 종류·UI 명칭을 재정비 (연속성 잠금 → **서약**, 세계 규칙 → **캐논**, 개체 상태 → **상태 보드**, 장면 감독 → **장면 연출**). 기존 저장 데이터는 자동 변환됩니다

## v0.3.0

- **인물 도감**: 등장인물 프로필(역할·나이·직업·외모·특성)과 인물별 관계 자동 추적, 카드 UI로 편집·수동 추가·주입 제외 가능. 매 턴 최신 인물 정보가 AI에게 전달됨. 인계에도 포함
- **커스텀 API**: 사서 전용 OpenAI 호환 API 직접 입력(URL/키/모델/온도/타임아웃) + 연결 테스트 버튼
- 용어 정리: "기록가" → **사서** (서고를 지키는 사서)

## v0.2.0

- 요약된 메시지 자동 숨김 (최근 N개 보존, 언제든 해제 가능)
- 다른 채팅으로 기억 인계 (요약·고정 기억·캐논·서약 누적 이어받기)
- 요약 언어 설정 (자동/한국어/영어/일본어/하이브리드)
- 장면 상태 자동 추적 (위치·시각·날짜를 상태 보드에 기록)
- 색인 범위 지정, 기록 실패 턴 추적·재시도
- 주입 토큰 사용량 실시간 표시
- 요약 프롬프트 개선 (인과 서술, 대사 원문 인용 유지)
